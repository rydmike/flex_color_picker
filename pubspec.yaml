name: flex_color_picker
description: A customizable Flutter primary color, accent color and custom color picker. Includes an optional HSV wheel color picker.
version: 1.0.0-dev.3
homepage: https://github.com/rydmike/flex_color_picker

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
